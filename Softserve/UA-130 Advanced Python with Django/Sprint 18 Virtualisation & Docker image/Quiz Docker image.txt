**Перевод вопроса:**  
Правда ли, что при контейнеризации гостевой операционной системы она запускается на той же версии операционной системы, что и хост?

**Правильный ответ:**  
**a. No**

---

### 🧠 Объяснение:

Контейнеры **не запускают отдельную гостевую ОС** так, как это делают виртуальные машины. Вместо этого:

- Контейнеры **разделяют ядро** с хостовой ОС.
- Они работают с **собственными библиотеками и пользовательским пространством**, но **не обязаны иметь ту же версию ОС, что и хост**.

---

### 📦 Примеры:

- Ты можешь запустить контейнер с образом **Ubuntu 18.04** на хосте с **Ubuntu 22.04**, **Debian**, или даже **Alpine Linux**.
- Главное, чтобы контейнер был **совместим с ядром хоста**, а не полностью идентичным по версии.

---

### ❗ Почему ответ **b. Yes** — неверен:

- Это путаница с виртуальными машинами (VMs), где действительно может быть разная операционная система.
- Контейнер может использовать образы с библиотеками, относящимися к любой поддерживаемой ОС: Alpine, Debian, Ubuntu, и т.д.

---

### 💬 Итого:

> Контейнер **не требует ту же версию ОС**, что и у хоста.  
> Он просто использует **ядро хост-машины**, а всё остальное — своё.

**Перевод вопроса:**  
Docker — это набор продуктов ________?

**Правильный ответ:**  
**b. PaaS** (Platform as a Service)

---

### 🤓 Объяснение:

**Docker** предоставляет набор инструментов и сервисов, которые помогают разработчикам создавать, упаковывать и запускать приложения в изолированной среде — **контейнерах**.

Это означает, что Docker предоставляет **платформу** (а не саму инфраструктуру или софт как услугу) для:

- управления контейнерами и образами;
- автоматизации CI/CD;
- оркестрации (например, с Docker Swarm или Kubernetes).

Вот почему:

- **✅ PaaS (Platform as a Service)** — именно это: Docker предоставляет **платформу** для разработки, тестирования и развертывания приложений.

---

### Почему остальные варианты неверны:

- **a. SaaS (Software as a Service)**  
  Нет — Docker не предоставляет готовое приложение как услугу (в отличие, например, от Google Docs).

- **c. IaaC (Infrastructure as a Code)**  
  Это скорее про инструменты вроде Terraform или Ansible. Docker можно использовать *в рамках* IaC, но сам по себе им не является.

- **d. IaaS (Infrastructure as a Service)**  
  Это уровень ниже: виртуальные машины, сети и хранилища (например, AWS EC2). Docker **не управляет инфраструктурой**, а работает поверх неё
  

**Перевод вопроса:**  
Как называются репозитории, которые содержат Docker-образы?

**Правильный ответ:**  
**d. Registry**

---

### 🧠 Объяснение:

- В мире Docker, **репозиторий образов** называется **Registry (реестр)**.

#### 🔹 Что такое Registry?
Это централизованное хранилище, где Docker-образы **сохраняются, извлекаются и делятся**.  
Примеры:

- [Docker Hub](https://hub.docker.com) — публичный Docker-регистр;
- GitHub Container Registry (`ghcr.io`);
- Google Container Registry (`gcr.io`);
- Частные Registry (напр., Nexus, Harbor, GitLab).

---

### 📦 Что происходит на практике:
```bash
docker pull nginx     # тянет образ из registry (обычно Docker Hub)
docker push myrepo/app:latest   # загружает в registry
```

---

### Почему другие варианты **неправильны**:

- **a. Storage** — общее слово, но не термин из Docker-экосистемы.
- **b. Cloud** — Docker registry может быть в облаке, но “Cloud” — не его название.
- **c. Images** — это содержимое registry, а не само хранилище.
- **e. Containers** — это **запущенные экземпляры образов**, а не образы или их репозитории.


**Перевод вопроса:**  
Какой командой создаётся Docker-образ из Dockerfile?

**Правильный ответ:**  
**b. docker build**

---

### 🧱 Объяснение:

Команда `docker build` — это **единственно верный способ создать образ из Dockerfile**.

**Пример использования:**
```bash
docker build -t my-image:latest .
```
- `-t` — присваивает имя и тег (например, `my-image:latest`)
- `.` — означает, что `Dockerfile` находится в текущей директории

---

### Почему другие варианты **неправильны**:

- **a. docker make**  
  Нет такой команды в Docker CLI вообще — это путаница с `make` из Unix-среды.

- **c. docker run**  
  Запускает **контейнер** из образа, но **не создаёт образ**. Используется на следующем этапе.

- **d. docker compose**  
  Это инструмент для управления **мультиконтейнерными приложениями** (на основе `docker-compose.yml`), но не для сборки образа напрямую (если только он не использует директиву `build:` внутри `docker-compose.yml`).

- **e. docker create**  
  Эта команда создаёт **контейнер**, но не запускает его и **не создаёт образ**. Её роль — другая.


**Перевод вопроса:**  
Как называются виртуальные машины в терминах виртуализации?

**Правильный ответ:**  
**c. guest**

---

### 🧠 Объяснение:

В контексте виртуализации:

- **Гостевая машина (Guest)** — это и есть виртуальная машина, которая работает **внутри** хостовой системы (на физическом сервере или хосте).
- Она использует виртуализированные ресурсы, предоставляемые гипервизором.

---

### 💡 Термины в связке:

| Термин         | Что это значит                                     |
|----------------|----------------------------------------------------|
| **Host**       | Физическая машина или ОС, на которой работает гипервизор |
| **Guest**      | Виртуальная машина, запускаемая на этом хосте      |
| **Hypervisor** | Прослойка, управляющая виртуальными машинами       |

---

### ❌ Почему другие варианты неверны:

- **a. computer** — слишком общее, не обозначает роль в виртуализации.
- **b. resource** — относится к CPU, RAM и т.д., но не к виртуальной машине.
- **d. host** — это как раз противоположность guest: физический/управляющий слой.
- **e. virtual host** — термин из веб-серверов (например, Apache, Nginx), не из виртуализации ОС.

---

Так что запоминай:  
> 🖥 Хост запускает — 🎛 Гость исполняет.


**Перевод вопроса:**  
Как называется Docker-образ, который выполняется на Docker-хосте?

**Правильный ответ:**  
**b. Container**

---

### 🐳 Объяснение:

- **Docker Image** — это просто **шаблон**, статичный и неизменяемый.
- Когда ты запускаешь образ (`docker run`), он становится **container** — изолированным, работающим процессом.

📦 Образ → 🏃 Контейнер  
```bash
docker run nginx         # образ nginx → запускается контейнер
```

---

### 🧠 Что значат остальные варианты:

| Вариант          | Почему неверен                                                 |
|------------------|----------------------------------------------------------------|
| **a. Docker Registry** | Это место, где **хранятся** образы (например, Docker Hub), а не то, что выполняется |
| **c. Image**           | Это "исходник", но именно запущенный экземпляр называется container |
| **d. Host**            | Это хост-система (сервер), на которой запускается Docker, а не образ |

---

> Итак:  
> **Образ (Image)** + запуск на хосте = **Контейнер (Container)**


**Перевод вопроса:**  
На какие операционные системы можно установить Oracle VirtualBox?

**Правильные ответы:**  
✅ **a. Windows**  
✅ **c. macOS**  
✅ **d. Linux**

---

### 🧠 Объяснение:

**Oracle VirtualBox** — это кроссплатформенный гипервизор, позволяющий запускать виртуальные машины на разных операционных системах-хостах. Он официально поддерживается на:

- 🖥 **Windows** — XP и новее (включая 10 и 11)
- 🍎 **macOS** — от 10.14 Mojave и новее
- 🐧 **Linux** — большинство дистрибутивов: Ubuntu, Debian, Fedora, CentOS, Arch и т.д.
- 🧪 Также есть сборка для Solaris (но этого варианта нет среди ответов)

---

### ❌ Почему другие варианты — **неверны**:

- **b. BeOS** — устаревшая и почти не используемая система. VirtualBox её **никогда официально не поддерживал**.
- **e. Android** — это мобильная ОС, не поддерживающая установку VirtualBox. **VirtualBox работает только на десктопных/серверных ОС**.

---

### 🧩 Итого:

| ОС         | VirtualBox поддерживается? |
|------------|-----------------------------|
| Windows     | ✅ Да                      |
| macOS       | ✅ Да                      |
| Linux       | ✅ Да                      |
| BeOS        | ❌ Нет                     |
| Android     | ❌ Нет                     |


**Перевод вопроса:**  
Какая команда используется для загрузки (скачивания) образа **Ubuntu** из Docker Hub?

**Правильный ответ:**  
✅ **a. `docker pull ubuntu`**

---

### 🐳 Объяснение:

- `docker pull` — это команда, которая **загружает Docker-образ** из реестра (обычно с [Docker Hub](https://hub.docker.com)).
- `ubuntu` — имя официального образа Ubuntu Linux, поддерживаемого Canonical.

**Пример:**
```bash
docker pull ubuntu
```

Результат — ты получаешь локальную копию образа, готовую к запуску.

---

### ❌ Почему другие варианты неверны:

| Вариант            | Объяснение                                                       |
|--------------------|------------------------------------------------------------------|
| **b. docker push** | Эта команда **загружает образ на Docker Hub**, а не скачивает его |
| **c. docker set**  | Такой команды в Docker вообще не существует                      |
| **d. docker get**  | Не существует в CLI Docker — это придуманный вариант             |

---

📦 После `docker pull ubuntu` можешь запускать контейнер:
```bash
docker run -it ubuntu bash
```


**Перевод вопроса:**  
Сколько уровней (слоёв) существует в традиционной архитектуре компьютерной системы?

**Правильный ответ:**  
**b. 4**

---

### 🧠 Объяснение:

Традиционная архитектура компьютерной системы обычно описывается как состоящая из **четырёх основных уровней (layers)**:

1. **Аппаратный уровень (Hardware Layer)**  
   – Физические компоненты: процессор, память, устройства ввода/вывода.

2. **Системное программное обеспечение (System Software Layer)**  
   – Операционная система, драйверы, BIOS — всё, что управляет железом.

3. **Прикладное программное обеспечение (Application Software Layer)**  
   – Программы, с которыми взаимодействует пользователь: браузеры, редакторы, игры.

4. **Пользовательский уровень (User Layer)**  
   – Сам пользователь и его взаимодействие с системой через интерфейс.

---

### ❌ Почему другие варианты неверны:

- **a. 2 / e. 3** — слишком упрощённо, не охватывают все ключевые уровни.
- **c. 6 / d. 5** — могут встречаться в более детализированных моделях, но **традиционная архитектура** обычно описывается через **4 слоя**.


**Перевод вопроса:**  
Что из следующего **не относится** к Docker-образу? **(Выберите два варианта)**

**Правильные ответы:**  
✅ **d. Is hardware**  
✅ **e. Being run on a Docker Host**

---

### 🧠 Объяснение:

Docker-образ — это **шаблон** файловой системы, содержащий всё необходимое для запуска приложения (код, библиотеки, зависимости). Но **он не запускается сам по себе** и **не является "железом"**.

---

### 🔍 Анализ вариантов:

| Вариант | Подходит ли к Docker-образу? | Объяснение |
|--------|-------------------------------|------------|
| **a. NOT A FILE SYSTEM** | ❌ _Неверно_ | Наоборот — **Docker image** содержит файловую систему. Это набор слоёв, из которых собирается root FS контейнера. |
| **b. Basically a tar file** | ✅ _Верно_ | Технически образы состоят из нескольких архивированных слоёв в формате tar. |
| **c. Has a hierarchy** | ✅ _Верно_ | Образы построены как иерархия слоёв (base image → новые слои с изменениями). |
| **d. Is hardware** | ❌ _НЕ верно (и это правильный ответ)_ | Docker-образ — это **ПО**, а не оборудование. Он не имеет отношения к физическому железу. |
| **e. Being run on a Docker Host** | ❌ _НЕ верно (и это правильный ответ)_ | Не образ запускается, а **контейнер**, созданный из образа. Образ — это "шаблон", а контейнер — "бегущий экземпляр". |
| **f. NOT VHD (virtual hard drive)** | ✅ _Верно_ | Образ **не является VHD**. Это своя система, основанная на tar-архивах и слоях, а не виртуальных дисках как в Hyper-V или VMWare. |

---

### 📦 Кратко:

- **Docker image ≠ контейнер**
- **Docker image ≠ оборудование (hardware)**
- Он — это **архив слоёв** с файловой системой, из которого можно создать и запустить контейнер


**Перевод вопроса:**  
Что **не является преимуществом виртуализации**? (Выберите два варианта)

**Правильные ответы:**  
✅ **a. This increases the cost of IT infrastructure**  
✅ **f. Requires investment in hardware support**

---

### 🧠 Объяснение:

Виртуализация — это технология, которая наоборот **снижает затраты**, **повышает гибкость**, и **эффективно использует ресурсы**. Давайте разберём каждый вариант:

---

### ✅ Что **НЕ является** преимуществом виртуализации:

- **a. This increases the cost of IT infrastructure**  
  ❌ _Неверно_: виртуализация как раз **уменьшает расходы**, позволяя запускать много виртуальных машин на одном сервере. Это снижает потребность в физическом оборудовании.

- **f. Requires investment in hardware support**  
  ❌ _Неверно_: наоборот, она **снижает необходимость в дополнительной поддержке железа**, поскольку меньше серверов = меньше обслуживания. Виртуализация работает на уже существующей инфраструктуре.

---

### ✅ Что действительно **является преимуществами**:

- **b. Enables running multiple operating systems**  
  ✔ Да, можно запускать Windows, Linux и др. параллельно на одной машине.

- **c. High availability and disaster recovery**  
  ✔ Виртуальные машины можно быстро перемещать, восстанавливать, клонировать — это упрощает бэкапы и отказоустойчивость.

- **d. Pay per use of the IT infrastructure on demand**  
  ✔ Это возможно при использовании виртуальных ресурсов в облаке — ты платишь только за то, что используешь.

- **e. Remote access and rapid scalability**  
  ✔ Легко масштабировать приложения и управлять ими удалённо — это стандарт для современных виртуализированных сред.

- **g. More flexible and efficient allocation of resources**  
  ✔ Можно динамически перераспределять CPU, RAM, диски между виртуальными машинами в зависимости от нагрузки.

---

🎯 В двух словах: виртуализация экономит деньги и ресурсы, а не требует больше “железа”.


**Перевод вопроса:**  
Какая команда используется для создания контейнера из Docker-образа?

**Правильный ответ:**  
✅ **e. `docker create`**

---

### 🐳 Объяснение:

Когда ты хочешь создать **контейнер** (ещё не запущенный) из существующего Docker-образа, ты используешь команду:
```bash
docker create <image_name>
```

Она **создаёт контейнер**, но **не запускает** его. Позже ты можешь запустить его отдельно через `docker start`.

📌 Пример:
```bash
docker create --name myubuntu ubuntu
docker start myubuntu
```

---

### ❌ Почему другие варианты неверны:

| Вариант            | Причина |
|--------------------|---------|
| **a. `docker push`** | Отправляет образ в реестр (например, Docker Hub), не имеет отношения к созданию контейнера |
| **b. `docker build`** | Создаёт **образ** из Dockerfile, а не контейнер |
| **c. `docker pull`** | Загружает образ из реестра, но не создаёт контейнер |
| **d. `docker make`** | Не существует такой команды в Docker CLI |

---

### 💬 Дополнительно:

- Альтернативный и часто используемый способ — **`docker run`**:
  ```bash
  docker run ubuntu
  ```
  Эта команда **и создаёт, и запускает** контейнер в один шаг. Но если спрашивают про только создание — `docker create` — правильный выбор.
