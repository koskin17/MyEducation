# Create a Pizza class with the attributes order_number and ingredients (which is given as a list). Only the ingredients will be given as input.

# You should also make it so that its possible to choose a ready made pizza flavour rather than typing out the ingredients manually! As well as creating this Pizza class, hard-code the following pizza flavours.

# Examples:
# p1 = Pizza(["bacon", "parmesan", "ham"])   # order 1
# p2 = Pizza.garden_feast()                  # order 2
# p1.ingredients ➞ ["bacon", "parmesan", "ham"]
# p2.ingredients ➞ ["spinach", "olives", "mushroom"]
# p1.order_number ➞ 1
# p2.order_number ➞ 2

# Задача требует создать класс Pizza, который:
# - Хранит атрибуты order_number и ingredients.
# - Позволяет создавать экземпляры как с пользовательскими ингредиентами, так и через готовые варианты (garden_feast, hawaiian, meat_festival).
# - Учитывает автоматическое увеличение order_number для каждого нового заказа.

class Pizza:
    # Общий счетчик для номеров заказов
    order_counter = 0

    def __init__(self, ingredients):
        """Создание нового объекта Pizza с указанными ингредиентами"""
        Pizza.order_counter += 1  # Увеличиваем номер заказа
        self.order_number = Pizza.order_counter
        self.ingredients = ingredients  # Сохраняем ингредиенты

    @classmethod
    def hawaiian(cls):
        """Создает пиццу Hawaiian с фиксированными ингредиентами"""
        return cls(["ham", "pineapple"])

    @classmethod
    def meat_festival(cls):
        """Создает пиццу Meat Festival с фиксированными ингредиентами"""
        return cls(["beef", "meatball", "bacon"])

    @classmethod
    def garden_feast(cls):
        """Создает пиццу Garden Feast с фиксированными ингредиентами"""
        return cls(["spinach", "olives", "mushroom"])


# ✅ **Примеры использования**
p1 = Pizza(["bacon", "parmesan", "ham"])  # order 1
p2 = Pizza.garden_feast()                 # order 2

print(p1.ingredients)  # ➞ ["bacon", "parmesan", "ham"]
print(p2.ingredients)  # ➞ ["spinach", "olives", "mushroom"]
print(p1.order_number) # ➞ 1
print(p2.order_number) # ➞ 2

#  Разбор решения
# 1️⃣ Атрибут order_counter
# - Я сделал order_counter статической переменной (Pizza.order_counter), чтобы каждый новый заказ получал уникальный номер.
# - Он увеличивается при каждом создании экземпляра Pizza.
# 2️⃣ Конструктор __init__
# - Принимает список ingredients и сохраняет его.
# - Присваивает уникальный номер заказа (self.order_number).
# 3️⃣ Методы класса @classmethod
# - hawaiian(), meat_festival(), garden_feast() создают предопределенные пиццы.
# - Используется cls(...), чтобы возвращать новый экземпляр Pizza с нужными ингредиентами.

# 🚀 Почему решение правильное?
# ✔ Соответствует условиям: можно создавать пиццу по готовым рецептам и с любыми ингредиентами.
# ✔ Сохраняет номера заказов: order_number увеличивается автоматически.
# ✔ Код структурирован: легко читаемый, масштабируемый.

p1 = Pizza(['bacon', 'parmesan', 'ham'])
print(p1.ingredients)

p2 = Pizza.garden_feast()
print(p2.ingredients)

p3 = Pizza.hawaiian()
print(p3.ingredients)

p4 = Pizza.meat_festival()
print(p4.ingredients)

p5 = Pizza(["pepperoni", "bacon"])
print(p5.ingredients)

my_pizza = Pizza(['cheese', 'caviar', 'oyster', 'uranium'])
print(my_pizza.ingredients)

print(p1.order_number)
print(p2.order_number)
print(p3.order_number)
print(p4.order_number)
print(p5.order_number)
print(my_pizza.order_number)

# Давай разберемся с методами классов (`cls`) простым языком.

# Представь, что у тебя есть чертеж для создания печенья (это наш класс). По этому чертежу можно сделать много разных печенек (это объекты класса).

# **Обычные методы (self):**

# * Это как действия, которые может выполнять каждая отдельная печенька. Например, метод `eat_me()`. Каждая печенька "знает", как ее съесть.
# * Эти методы всегда принимают первым аргументом `self` - ссылку на конкретную печеньку, с которой мы работаем.
# * Они используются, когда тебе нужно что-то сделать с конкретной печенькой, используя ее уникальные свойства (например, ее размер, глазурь и т.д.).

# **Методы классов (cls):**

# * Это как действия, которые относятся ко всему "чертежу печенья" в целом, а не к какой-то конкретной печеньке. Например, метод `set_default_flour('пшеничная')`. Ты говоришь всему чертежу, что по умолчанию нужно использовать пшеничную муку для всех будущих печенек, если не указано иное.
# * Эти методы принимают первым аргументом `cls` - ссылку на сам класс (на наш чертеж печенья).
# * Они используются, когда тебе нужно сделать что-то, что влияет на сам класс или связано с ним, а не с конкретным объектом.

# **Когда используются методы классов (`cls`) и в чем их выгода?**

# 1.  **Создание объектов альтернативными способами (альтернативные конструкторы):**
#     * Представь, что у тебя есть информация о печеньке в каком-то нестандартном формате (например, строка с размерами). Ты можешь написать метод класса, который примет эту строку, разберет ее и создаст новый объект печеньки, используя стандартный конструктор класса.
#     * **Выгода:** Позволяет создавать объекты более гибко, из разных источников данных, не полагаясь только на стандартный конструктор (`__init__`).

#     ```python
#     class Печенье:
#         def __init__(self, вкус, размер):
#             self.вкус = вкус
#             self.размер = размер

#         @classmethod
#         def из_строки_размеров(cls, строка_размеров):
#             размер, вкус = строка_размеров.split('-')
#             return cls(вкус, размер) # 'cls' здесь - это сам класс 'Печенье'

#     печенька1 = Печенье('шоколадное', 'большое')
#     печенька2 = Печенье.из_строки_размеров('маленькое-ванильное')
#     print(печенька2.вкус, печенька2.размер) # Выведет: ванильное маленькое
#     ```

# 2.  **Работа с атрибутами класса (если они есть):**
#     * У класса может быть какая-то общая информация, которая относится ко всем печенькам (например, название компании-производителя). Метод класса может изменять или получать эту общую информацию.
#     * **Выгода:** Позволяет централизованно управлять информацией, общей для всех объектов класса.

#     ```python
#     class Печенье:
#         производитель = "Сладкая фабрика"

#         def __init__(self, вкус, размер):
#             self.вкус = вкус
#             self.размер = размер

#         @classmethod
#         def сменить_производителя(cls, новый_производитель):
#             cls.производитель = новый_производитель

#     print(Печенье.производитель) # Выведет: Сладкая фабрика
#     печенька1 = Печенье('шоколадное', 'большое')
#     print(печенька1.производитель) # Выведет: Сладкая фабрика

#     Печенье.сменить_производителя("Новый кондитер")
#     print(Печенье.производитель) # Выведет: Новый кондитер
#     печенька2 = Печенье('ванильное', 'маленькое')
#     print(печенька2.производитель) # Выведет: Новый кондитер
#     ```

# 3.  **Создание фабричных методов:**
#     * Метод класса может решать, какой именно подкласс (если есть наследование) нужно создать в зависимости от каких-то условий.
#     * **Выгода:** Обеспечивает более гибкое создание объектов в иерархии классов.

#     ```python
#     class Транспорт:
#         def __init__(self, скорость):
#             self.скорость = скорость

#         @classmethod
#         def создать_быстрый(cls):
#             return cls(100)

#         @classmethod
#         def создать_медленный(cls):
#             return cls(20)

#     автомобиль = Транспорт.создать_быстрый()
#     велосипед = Транспорт.создать_медленный()
#     print(автомобиль.скорость) # Выведет: 100
#     print(велосипед.скорость) # Выведет: 20
#     ```

# **Ключевое отличие:** Обычные методы работают с конкретным объектом (`self`), а методы класса работают с самим классом (`cls`).

# Использование методов класса делает ваш код более организованным, читаемым и позволяет решать определенные задачи, связанные с созданием объектов и управлением информацией на уровне класса.