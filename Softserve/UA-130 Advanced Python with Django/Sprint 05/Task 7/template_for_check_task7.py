# ## 🎯 Что это будет:

# Это будет **мини-тестировщик**, который:

# * запускает серию тестов с заранее заданными входными данными;
# * проверяет, вернулся ли **ожидаемый результат** или возникло **исключение**;
# * показывает:

#   * ✅ «Тест пройден» — если результат верный;
#   * ❌ «Ошибка» — если результат не совпадает с ожидаемым.

# ---

# ## 🧪 Что ты сможешь проверять:

# * Что функция возвращает правильную сумму — для валидных входных данных.
# * Что выбрасываются исключения `MyExceptions` — если:

#   * индексы отрицательные;
#   * индексы за пределами массива;
#   * элементы по этим индексам — **не числа**;
#   * сами индексы — **не числа**.

## ✅ Готовый шаблон для проверки:

class MyExceptions(Exception):
    pass

def sum_slice_array(arr, first, second):
    if not (isinstance(first, int) and isinstance(second, int) and first >= 1 and second >= 1):
        raise MyExceptions("Numbers must be integers and at least 1")

    if first > len(arr) or second > len(arr):
        raise MyExceptions("Indexes are out of range")

    if not (isinstance(arr[first - 1], (int, float)) and isinstance(arr[second - 1], (int, float))):
        raise MyExceptions("Elements must be numbers")

    return float(arr[first - 1] + arr[second - 1])

# 🎯 Тесты: (входные данные, ожидаемый результат)
tests = [
    (([1, 2, 3], 1, 2), 3.0),
    (([10, 5, 15], 2, 3), 20.0),
    (([1, "string", 3], 1, 2), "MyExceptions"),
    (([1, 2, 3], -1, 2), "MyExceptions"),
    (([1, 2, 3], 1, 10), "MyExceptions"),
    (([1, None, 3], 2, 3), "MyExceptions"),
    (([0.5, 1.5], 1, 2), 2.0),
]

# 🚀 Прогон тестов
for i, (args, expected) in enumerate(tests, 1):
    try:
        result = sum_slice_array(*args)
        if result == expected:
            print(f"✅ Тест {i} пройден: результат = {result}")
        else:
            print(f"❌ Тест {i} НЕ пройден: результат = {result}, ожидалось = {expected}")
    except MyExceptions:
        if expected == "MyExceptions":
            print(f"✅ Тест {i} пройден: корректно сработало исключение")
        else:
            print(f"❌ Тест {i} НЕ пройден: возникло исключение, ожидалось {expected}")

# ## 📋 Пример вывода:

# ```
# ✅ Тест 1 пройден: результат = 3.0
# ✅ Тест 2 пройден: результат = 20.0
# ✅ Тест 3 пройден: корректно сработало исключение
# ✅ Тест 4 пройден: корректно сработало исключение
# ✅ Тест 5 пройден: корректно сработало исключение
# ✅ Тест 6 пройден: корректно сработало исключение
# ✅ Тест 7 пройден: результат = 2.0
# ```

# ---

# ## 🧠 Преимущества шаблона:

# * Прост в использовании;
# * Помогает проверять любые входные данные;
# * Даёт уверенность, что всё работает правильно.

# Хочешь, я сделаю отдельную версию этого шаблона с логированием всех вызовов?
