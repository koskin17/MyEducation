## ✅ Що в тебе зараз є:
* `server.py` — твій сервер із SQLite, обробляє GET, POST, PUT, DELETE.
* `client.py` — клієнт, який надсилає запити до сервера.
* Обидва файли лежать у тій самій папці ✅
* Сервер запущений, наприклад:

  ```
  http://localhost:8000
  ```

---

## ✅ Наступний крок — запустити `client.py`

### 🔧 Просто виконай:
```bash
python client.py
```
### 🔍 Що відбудеться:
Твій `client.py`:
1. 📤 Надішле POST-запит — створить нового користувача.
2. 📥 Зробить GET — отримає список користувачів.
3. 🔄 Зробить PUT — оновить дані користувача.
4. ❌ Зробить DELETE — видалить користувача.
5. 💾 Збереже список у файл `saved_users.txt`.

---

## 📝 Як перевірити, що все працює:

1. 🔄 Перевір у **консолі**, що всі статуси — `200`, `201`, або `204`.
2. 🔍 Відкрий файл `saved_users.txt` (лежить у тій же папці) — там має бути JSON:

   ```json
   [
     {
       "id": 1,
       "username": "admin",
       "firstName": "John"
     },
     ...
   ]
   ```
3. 🔁 Ти можеш повторити запуск `client.py` скільки завгодно разів — головне, щоб сервер був запущений.

---

## 🧪 Хочеш більше перевірок?

* Виводь кількість користувачів після GET.
* Виводь повідомлення "Користувача не знайдено", якщо DELETE повертає 404.
* Або протестуй API за допомогою `unittest`.

---

## 📦 Наступні кроки (за бажанням):

| Ідея                         | Що зробити                       |
| ---------------------------- | -------------------------------- |
| 📋 Логувати відповіді у файл | Записуй час + статус у `log.txt` |
| 🌐 Зробити HTML-інтерфейс    | Простий сайт для API             |
| 🧪 Написати тести            | `unittest`, `pytest` для API     |
| 🛠️ Підключити frontend      | React, Vue, Flask-Templates      |

---

🎉 Якщо все працює — вітаю тебе з повноцінною робочою зв'язкою **сервер + клієнт + база даних**!
Це вже те, що роблять **бекенд-розробники** у справжніх проєктах.

Готовий допомогти з наступними кроками — тести, HTML-інтерфейс, підключення до фронтенду або хостинг. Ти на правильному шляху 🚀
