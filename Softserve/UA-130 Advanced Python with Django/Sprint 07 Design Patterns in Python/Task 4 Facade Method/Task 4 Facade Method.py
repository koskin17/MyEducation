# Imagine we have a washing machine which can wash the clothes, rinse the clothes and spin the clothes but all the tasks separately. We need a system that can automate the whole task without the disturbance or interference of us. 
# To solve the above-described problem, we would like to hire the Facade Method. It will help us to hide or abstract the complexities of the subsystems as follows.
# Note: the methods wash(), rinse() and spin() provide the output of the appropriate operation.

# Задача как раз на очень полезный паттерн **Facade (Фасад)** — мы его сейчас и реализуем ✅

## 📌 Перевод задачи на русский
# > У нас есть стиральная машина, которая умеет:
# > * стирать (`wash()`),
# > * полоскать (`rinse()`),
# > * отжимать (`spin()`),
# >
# > Но всё это — **отдельные действия** (методы).
# >
# > 🔹 Требуется **автоматизировать весь процесс**, чтобы пользователь не управлял каждым этапом вручную.
# >
# > Для этого мы используем **паттерн проектирования “Фасад” (Facade)**, чтобы **скрыть внутреннюю сложность** и предоставить **одну простую команду** — например, `start()`.

## 🧠 Объяснение: Что такое Facade и как он работает?

# **Facade** — это паттерн, который:
# * объединяет **много подсистем** под одной “обёрткой”;
# * **скрывает сложность**;
# * даёт простой и понятный интерфейс для пользователя.

### 🔧 Почему это удобно?
# * Пользователь не думает, в каком порядке что вызывать.
# * Всё делается одной командой, но под капотом могут быть 3, 5, 100 шагов.

## ✅ Шаги решения
### 1. Создаём **классы подсистем**:
# * Класс `Washer` с методом `wash()`;
# * Класс `Rinser` с методом `rinse()`;
# * Класс `Spinner` с методом `spin()`.

# Каждый из них **умеет делать только одну работу**.

### 2. Создаём **класс Facade** — `WashingMachine`
# * Он будет **создавать объекты** всех этих классов;
# * У него будет метод `start()`, который **вызовет все этапы по порядку**.

## ✅ Полный код решения
# Subsystems
class Washer:   #Class for wash the clothes
    def wash(self):
        print("Washing...")

class Rinser:   # Class for rinse the clothes
    def rinse(self):
        print("Rinsing...")

class Spinner:  # Class for spin the clothes
    def spin(self):
        print("Spinning...")

# Facade
class WashingMachine:   # Class as Facade
    def __init__(self):
        self.washer = Washer()
        self.rinser = Rinser()
        self.spinner = Spinner()

    def startWashing(self):
        self.washer.wash()
        self.rinser.rinse()
        self.spinner.spin()

# Code for client
if __name__ == "__main__":
    machine = WashingMachine()
    machine.startWashing()

## 📦 Что происходит при запуске:
# Starting washing machine...
# Washing the clothes
# Rinsing the clothes
# Spinning the clothes
# Clothes are clean!

## 💡 Почему это круто?
# * Ты можешь изменить, в каком порядке что-то делается, **внутри класса `WashingMachine`**, а пользователь об этом **даже не узнает**.
# * Это идеально для **API, библиотек, систем с внутренней логикой**, которую надо упростить для конечного пользователя.

### 📌 Разбор кода:
# class WashingMachine:  # Фасад
#     def __init__(self):
#         self.washer = Washer()
#         self.rinser = Rinser()
#         self.spinner = Spinner()
# 🔹 Этот класс `WashingMachine` — **фасад**, то есть "обёртка", которая управляет более сложной системой.

### **1. `machine = WashingMachine()`**
# Ты создаёшь **объект класса `WashingMachine`**, то есть:
# machine → экземпляр фасада

### **2. Ему стал доступен метод `start()` из класса `WashingMachine`**
# ✅ Да, всё верно.
# Ты можешь вызвать:
# machine.start()
# Потому что этот метод определён прямо в классе `WashingMachine`.

### **3. Что делает `__init__()`?**
# Вот тут главное:
# def __init__(self):
#     self.washer = Washer()
#     self.rinser = Rinser()
#     self.spinner = Spinner()

# 🔹 При создании объекта `machine`:
# * Python вызывает метод `__init__()` — **конструктор**.
# * Внутри конструктора создаются **другие объекты**:

#   * `self.washer` — это экземпляр класса `Washer`
#   * `self.rinser` — это экземпляр класса `Rinser`
#   * `self.spinner` — экземпляр класса `Spinner`

# ⚠️ Эти методы `wash()`, `rinse()`, `spin()` **не становятся напрямую методами `machine`**, но `machine` может вызвать их **через атрибуты**:
# self.washer.wash()  →  объект washer вызывает свой метод wash()

## 🧠 Простыми словами:
# > Когда ты создаёшь `WashingMachine()`,
# > внутри неё создаются **три помощника**: `Washer`, `Rinser`, `Spinner`.

# А потом метод `start()` говорит:
# > Ребята, поехали:
# >
# > * Сначала `washer.wash()`
# > * Потом `rinser.rinse()`
# > * И в конце `spinner.spin()`

## 💬 Итог:
# | Ты сказал                                        | Правильно ли?                           |
# | ------------------------------------------------ | --------------------------------------- |
# | Объект `machine` создан                          | ✅ Да                                    |
# | Ему доступен `start()`                           | ✅ Да, напрямую                          |
# | Он получил доступ к методам                      | ⚠️ Через атрибуты: `self.washer.wash()` |
# | Метод `__init__` создаёт "внутри" другие объекты | ✅ Точно так!                            |

# Фасад с аргументами, ошибками или классом для "умной стиралки" с дополнительными режимами.

# Давай сделаем **умную стиральную машину с фасадом**, которая:
# 1. 💡 **Принимает аргументы** — режимы стирки (например: `"быстрая"`, `"деликатная"`).
# 2. ⚠️ **Обрабатывает ошибки** — если передан неизвестный режим.
# 3. 🧠 **Автоматизирует процесс**, но при этом остаётся простой для пользователя.

## 🎯 Что мы сделаем:
# * Класс `Washer` — умеет стирать, но по-разному в зависимости от режима.
# * Класс `Rinser` — может делать обычное или щадящее полоскание.
# * Класс `Spinner` — умеет крутить на разной скорости.
# * Класс `WashingMachine` (фасад) — принимает режим и **сам решает**, какие действия вызывать и как.

## ✅ Полный код с комментариями
# Подсистемы
# class Washer:
#     def wash(self, mode):
#         if mode == "быстрая":
#             print("🧼 Быстрая стирка: 15 минут.")
#         elif mode == "деликатная":
#             print("🧺 Деликатная стирка: 30 минут.")
#         elif mode == "интенсивная":
#             print("💪 Интенсивная стирка: 60 минут.")
#         else:
#             raise ValueError(f"Неизвестный режим стирки: {mode}")

# class Rinser:
#     def rinse(self, mode):
#         if mode == "деликатная":
#             print("💧 Щадящее полоскание.")
#         else:
#             print("💧 Обычное полоскание.")

# class Spinner:
#     def spin(self, mode):
#         if mode == "деликатная":
#             print("🔄 Отжим на низкой скорости.")
#         elif mode == "интенсивная":
#             print("🔄 Отжим на высокой скорости.")
#         else:
#             print("🔄 Стандартный отжим.")

# # Фасад
# class WashingMachine:
#     def __init__(self):
#         self.washer = Washer()
#         self.rinser = Rinser()
#         self.spinner = Spinner()

#     def start(self, mode):
#         """Запуск стирки в заданном режиме"""
#         try:
#             print(f"\n🟢 Запуск стирки в режиме: {mode.upper()}")
#             self.washer.wash(mode)
#             self.rinser.rinse(mode)
#             self.spinner.spin(mode)
#             print("✅ Стирка завершена!\n")
#         except ValueError as e:
#             print(f"❌ Ошибка: {e}")

# # Тестируем
# if __name__ == "__main__":
#     machine = WashingMachine()
    
#     machine.start("быстрая")       # Все подстроено под быстрый режим
#     machine.start("деликатная")    # Щадящие действия
#     machine.start("интенсивная")   # Самый мощный режим
#     machine.start("ночной")        # Ошибка — такого режима нет

## 📌 Что будет при запуске
# 🟢 Запуск стирки в режиме: БЫСТРАЯ
# 🧼 Быстрая стирка: 15 минут.
# 💧 Обычное полоскание.
# 🔄 Стандартный отжим.
# ✅ Стирка завершена!


# 🟢 Запуск стирки в режиме: ДЕЛИКАТНАЯ
# 🧺 Деликатная стирка: 30 минут.
# 💧 Щадящее полоскание.
# 🔄 Отжим на низкой скорости.
# ✅ Стирка завершена!


# 🟢 Запуск стирки в режиме: ИНТЕНСИВНАЯ
# 💪 Интенсивная стирка: 60 минут.
# 💧 Обычное полоскание.
# 🔄 Отжим на высокой скорости.
# ✅ Стирка завершена!


# 🟢 Запуск стирки в режиме: НОЧНОЙ
# ❌ Ошибка: Неизвестный режим стирки: ночной

## 🧠 Пояснение всего кода
### 1. Подсистемы
# * `Washer.wash(mode)` — проверяет режим и выводит соответствующее действие;
# * `Rinser.rinse(mode)` — подстраивается под режим;
# * `Spinner.spin(mode)` — отжимает по-разному.

# Все они **могут быть сложными внутри**, но пользователь об этом **не знает** — и это отлично.

### 2. Фасад `WashingMachine`
# * Хранит в себе объекты подсистем.
# * Предоставляет один метод `start(mode)`, который **инкапсулирует** всю сложную логику.
# * Обрабатывает ошибку, если передан **неизвестный режим**.

### 3. Гибкость
# Добавить новый режим? Просто в `Washer`, `Rinser`, `Spinner` — и фасад останется тем же.
# Изменить поведение одного режима? Меняем в одном месте — фасад всё скроет.

## 🔚 Вывод
# * Ты скрыл сложность системы (разные режимы, поведение) за **одной точкой входа**.
# * Ты **гарантируешь правильный порядок** выполнения.
# * Ты **обрабатываешь ошибки красиво**.

# Это и есть 💯 **паттерн Фасад в действии**.